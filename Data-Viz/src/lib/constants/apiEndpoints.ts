export const API_BASE_URL = process.env.API_BASE_URL || ''; // Set in .env file

export const API_ENDPOINTS = {
  ROUTES: '/routes',
  STOPS: '/stops',
  DELAYS: '/delays',
  PLAN_TRIP: '/plan-trip',
};
